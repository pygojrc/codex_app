# Source Map

- `AGENTS.md`：项目硬边界。
- `.gitignore`：Git 大文件、缓存和构建产物边界。
- `.ms/CURRENT.md`：当前任务状态。
- `.ms/rules/`：当前规则。
- `.ms/adr/`：长期决策。
- `.ms/tasks/01_chatgpt_26_715_72359_linux/`：当前适配计划与运行证据。
- `src/`：后续通用构建实现。
- `versions/`：后续版本配置与版本专属补丁。
- `inputs/`：后续项目内输入副本与 manifest。
- `runtime/`：后续 Electron runtime 下载、解包和 manifest。
- `native/`：后续 native module 可复现构建环境。
- `build/`、`dist/`、`tmp/`、`.cache/`：后续忽略的大文件或临时产物。
