# 2026-07-23 ChatGPT Adapter 独立项目规划记录

## 用户边界更新

- 用户指定使用 `/data/projects/Local/codex_app/chatgpt_adapter/` 作为新项目根。
- 所有适配相关源码、文档、Electron runtime、native modules、输入、产物和临时
  文件都必须位于该项目根内。
- 项目使用独立 Git 管理，并忽略非关键大文件、缓存、临时文件和可重建产物。

## 本轮完成

- 建立项目级 `AGENTS.md`、`.gitignore`、README 和 `.ms` 工作区。
- 记录“独立项目根与可复现输入”ADR。
- 制定项目目录、Git 跟踪边界、项目内缓存环境和七阶段实施计划。
- 旧的上级 `.ms/tasks/14_chatgpt_26_715_72359_linux_adaptation/` 保留为历史
  调查证据，不再作为当前实现入口。

## 本轮未执行

- 未复制 DMG。
- 未下载 Electron 42.3.0。
- 未构建 native modules。
- 未迁移或实现适配代码。
- 未构建、安装或启动 GUI。

## Git 初始化与边界验证

- 已初始化独立 Git 仓库，默认分支为 `main`。
- 初始提交：`415b42b chore(project): 初始化独立适配项目`。
- `git diff --cached --check` 在提交前通过。
- `git check-ignore` 已确认以下内容会被忽略：
  - `inputs/**/*.dmg`
  - `runtime/**/downloads/`
  - `runtime/**/extracted/`
  - `native/**/node_modules/`
  - `native/**/artifacts/`
  - `build/`
  - `dist/*.tar.zst`
- `.ms/CURRENT.md` 等源码与文档不在忽略范围。

## 2026-07-23 开始实施

- 用户要求开始执行，并明确 Electron runtime 必须从 Electron 官方 GitHub
  Releases 下载。
- 新增极简脚本：
  - `scripts/准备_DMG输入.py`
  - `scripts/下载_Electron_runtime.py`
  - `scripts/项目环境.sh`
- Electron 下载脚本固定使用
  `https://github.com/electron/electron/releases/download/v<version>/`，同时下载
  官方 `SHASUMS256.txt` 并在解包前校验目标资产。
- 当前任务状态更新为 `in_progress`。

## 2026-07-23 首轮实现与验收

- 项目内复制并校验 DMG：
  `05a76850a5a035bb3e7e3c0b61b7cb0f359c2509e8ec1ed4810526a6bc751abe`。
- 从 Electron 官方 GitHub Release 下载 42.3.0 Linux x64，首次断点残片被
  SHA256 拒绝；完善按总长度续传后，最终校验为
  `487a667ca6a734b958c16cff1df74d9d44d2c18a6cccdb4dd51f6301a356c420`。
- native modules 在 Electron ABI 146 下从源码重建并实际加载：
  `better-sqlite3`、`node-pty`、`@serialport/bindings-cpp`、`node-hid`。
  `node-hid` 同时生成 `HID.node` 与 Linux 默认使用的 `HID_hidraw.node`。
- `better-sqlite3 12.9.0` 源码不兼容 Electron 42.3.0 的 V8 external pointer
  tag API；采用 12.11.1 构建二进制，只替换 DMG 12.9.0 JS 下的 `.node`。
- 当前 pnpm 11.3.0 在 Node 26 + 代理环境下访问 registry 失败，而 Node fetch、
  curl、npm 均正常。构建入口保留 pnpm 默认，并增加显式 npm 项目内后备。
- 重新锚定 18 个补丁：主进程 9、worker 8、Open with 1；真实新 bundle
  每项均精确命中一次。Spark 两个旧 workaround 未迁移，等待真实请求复现。
- 六个插件从当前 DMG 白名单组装；清除 Darwin prebuild 和 macOS Chrome host。
  Latex 的 Tectonic 0.16.9 由官方 GitHub Release Linux musl x64 资产替换，
  SHA256 为
  `60b13a0826ae7ad9ce34b4a2df06bff2cfcfa6dda8a915477c0cbb84e1a4a902`。
- `codex`、`codex-code-mode-host`、`codex_chronicle`、`rg`、`cua_node`、
  `native/` 和 `default_app` 均有显式 replace/exclude 决策；发行包无 Mach-O。
- 静态构建与 tar.zst 打包通过，4 个离线回归测试通过。
- GUI 在无 DISPLAY 的 headless 环境完成两条 15 秒冒烟：
  - 端口 18766 未监听时，由应用启动 `/data/bin/codex` app-server。
  - 端口 18765 已监听时，直接连接 `ws://127.0.0.1:18765`。
- 动态日志无 `MODULE_NOT_FOUND`、`ERR_DLOPEN_FAILED`、语法错误或未处理
  Promise；仅有无桌面会话预期的 DBus/GTK 警告。
- 当前状态更新为 `done_first_pass`；保留真实桌面交互和 Spark 请求作为后续人工
  验收。
